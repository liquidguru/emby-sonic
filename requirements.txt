fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20.0
httpx>=0.27.0
pydantic-settings>=2.2
numpy>=1.26
librosa>=0.10
soundfile>=0.12
faiss-cpu>=1.8.0
transformers>=4.42.0
huggingface-hub>=0.23.0
scipy>=1.13.0
scikit-learn>=1.5.0
panns-inference>=0.1.1

# PyTorch CPU-only (smaller, fine for N100):
# pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.3.0

# NOTE: Essentia is NOT a core dependency. It has no reliable wheel on Windows
# or ARM, so requiring it would break native installs on those platforms.
# It is an OPTIONAL upgrade for higher-quality mood/vocal features — see
# requirements-optional.txt. Without it, librosa-derived proxies are used.
